<UserControl x:Class="IMDB_Project.Views.DirectorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IMDB_Project.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--header-->
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="Directors" Style="{StaticResource subHeaderStyle}"/>

            <!--search bar-->
            <Label Style="{StaticResource searchLabelStyle}">Search Directors:</Label>
            <TextBox 
            Margin="10" 
            Width="300" HorizontalAlignment="Left"
            Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
            Style="{StaticResource SearchStyle}" />
        </StackPanel>

        <ScrollViewer Grid.Row="2"  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <DataGrid ItemsSource="{Binding FilteredDirectors}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      CanUserDeleteRows="False" 
                      Style="{StaticResource TransparentDataGridStyle}"
                       Height="500">
                <DataGrid.Columns>
                    <!--Director Name-->
                    <DataGridTextColumn Binding="{Binding PrimaryName}" FontSize="16" Width="200">
                        <DataGridTextColumn.Header>
                            <StackPanel >
                                <TextBlock Text="Title" Style="{StaticResource DataGridHeaderStyleRating}"/>
                            </StackPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>

                    <!-- Birth Year -->
                    <DataGridTextColumn Binding="{Binding BirthYear}" Width="150" FontSize="14">
                        <DataGridTextColumn.Header>
                            <TextBlock Text="Birth Year" Style="{StaticResource DataGridHeaderStyleRating}"/>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>

                    <!-- Death Year -->
                    <DataGridTextColumn Binding="{Binding DeathYear}" Width="100" FontSize="14">
                        <DataGridTextColumn.Header>
                            <TextBlock Text="Death" Style="{StaticResource DataGridHeaderStyleRating}"/>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>
